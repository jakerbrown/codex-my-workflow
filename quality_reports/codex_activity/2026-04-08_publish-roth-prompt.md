Worked on publishing the Philip Roth prompt file so it can be linked from the
website.

Confirmed the prompt lives in `explorations/philip-roth-author-similarity/` and
prepared a minimal publish step with durable notes for the repo.

This matters because the website subtitle can now point to a real public prompt
file instead of a placeholder.

Verified the file path locally. Final verification still requires committing and
pushing the prompt file to GitHub.
