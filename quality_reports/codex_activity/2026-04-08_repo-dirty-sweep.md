Worked through the GitHub repos available to Codex and checked which matching local clones had uncommitted changes. I compared the GitHub-accessible repo list against local git directories under `/Users/jacobbrown/Documents/GitHub/`, then used git status and remote inspection to separate clear matches from ambiguous clones. This mattered because it gives a reliable list of repos with local work still sitting outside commits. Verified the matched dirty repos and noted a few local clones with name matches but different origins.
